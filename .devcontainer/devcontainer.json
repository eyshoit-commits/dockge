{
  "name": "Dockge DevContainer",
  "image": "node:22-bookworm-slim",
  "customizations": {
    "vscode": {
      "extensions": ["ms-azuretools.vscode-docker"]
    }
  },
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "remoteUser": "root",
  "postCreateCommand": "apt update && apt install -y curl ca-certificates gnupg unzip dumb-init && npm install -g tsx && npm install"
}
